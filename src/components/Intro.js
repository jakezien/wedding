import React from "react"
import Container from "../components/Container"

const Intro = () => (
  <section id="intro">
    <Container>
      <center>We're so excited you're here! We can't wait to celebrate with you as we open a new chapter in our life together.</center>
    </Container>
  </section>
)

export default Intro

 
